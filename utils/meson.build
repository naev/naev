subdir('build')

gen_version = find_program('build/gen_version.py')

# This one is a string so that meson uses the commited version to build the dist package
add_to_package = find_program('build/add_to_package.py')
