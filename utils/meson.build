gen_version = find_program('build/gen_version.sh')
gen_languages = find_program('build/gen_languages.sh')

# This one is a string so that meson uses the commited version to build the dist package
add_to_package = 'utils/build/add_to_package.sh'
