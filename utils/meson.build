gen_version = find_program('build/gen_version.sh')

# These are strings so that meson uses the commited version to build the dist package
add_to_package = 'utils/build/add_to_package.sh'
gen_languages = 'tail -n +2 meson.source_root()/po/LINGUAS > meson.source_root()/dat/LANGUAGES'
