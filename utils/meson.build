subdir('build')

cargo_lock = find_program(files('build'/'cargo_lock.sh'))
gen_version = find_program(files('build'/'gen_version.py'))

# This one is a string so that meson uses the commited version to build the dist package
add_to_package = find_program(files('build'/'add_to_package.py'))
