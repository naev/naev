--- src/input.c
+++ src/input.c
@@ -1567,7 +1567,6 @@ void input_handle( SDL_Event* event )
    if (gui_handleEvent(event))
       return;
 
-   input_log("input.c: input_handle", event);
    switch (event->type) {
 
       /*
