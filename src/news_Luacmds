	news.getFaction(factionname)
		gets a table with all articles of a faction

	news.getDate(time1,time2)
		gets all articles between time1 & time2

	news.getTitle(article)
		gets the title of an article

	news.newArticle(faction,title,content)
		puts a new news article into the stack,returns luaarticle

	news.freeArticle(article)
		removes article from stack and frees it

	news.__eq(article1,article2)
		checks if article1==article2

	news.dummy()
		adds a dummy Generic article

	news.getTitleOA
		gets the title of an article, very likely temporary
